import { CircleHelp, FileText, MoreHorizontal, Settings, ChevronDown } from "lucide-react";
import { navItems } from "./data";

export function Sidebar({ activeNav, onNavigate }: { activeNav: string; onNavigate: (label: string) => void }) {
  return <aside className="sidebar"><div className="brand"><div className="logo-mark"><span className="logo-eye" /></div><span>Hire<span className="brand-accent">Lens</span></span></div><div className="workspace-switcher"><div className="workspace-avatar">A</div><div><p>Acme, Inc.</p><small>Recruiting team</small></div><ChevronDown size={15} /></div><p className="nav-label">WORKSPACE</p><nav>{navItems.map(({ label, icon: Icon, count }) => <button key={label} onClick={() => onNavigate(label)} className={`nav-item ${activeNav === label ? "active" : ""}`}><Icon size={18} strokeWidth={1.8} /><span>{label}</span>{count && <em>{count}</em>}</button>)}</nav><p className="nav-label nav-spaced">MANAGE</p><nav><button className="nav-item"><FileText size={18} strokeWidth={1.8} /><span>Scorecards</span></button><button className="nav-item"><Settings size={18} strokeWidth={1.8} /><span>Settings</span></button></nav><div className="sidebar-bottom"><div className="help-row"><CircleHelp size={17} /><span>Help center</span></div><div className="profile-row"><div className="profile-avatar">AR</div><div><p>Alex Rivera</p><small>Admin</small></div><MoreHorizontal size={17} /></div></div></aside>;
}
